final class ProjectStrings {

  //MainPage BottomAppBar
  final String homepage = 'Home Page';
  final String favorites = 'Favorites';
  final String messages = 'Messages';
  final String profile = 'Profile';
  final String postad = 'Post Ad';

  final String signbuttontext = 'Log In';

  // Direct to LoginPage messages
  final String loginforfav = "Please log in to access favorites.";
  final String loginformes = "Please log in to access messages.";
  final String loginforprofile = "Please log in to access profile.";
}
/* 
class AppStrings {
  static final AppStrings _instance = AppStrings._internal();

  factory AppStrings() {
    return _instance;
  }

  AppStrings._internal();

  String get welcomeMessage => "Merhaba, Hoş Geldiniz!";
  String get loginButtonLabel => "Giriş Yap";
  String get registerButtonLabel => "Kayıt Ol";
  // Diğer metin değerleri buraya eklenebilir
} */

/* class AppStyles {
  static final AppStyles _instance = AppStyles._internal();

  factory AppStyles() {
    return _instance;
  }

  AppStyles._internal();

  TextStyle get welcomeMessageStyle => TextStyle(fontSize: 20, fontWeight: FontWeight.bold);
  TextStyle get loginButtonTextStyle => TextStyle(fontSize: 18, color: Colors.white);
  TextStyle get registerButtonTextStyle => TextStyle(fontSize: 18, color: Colors.blue);
  // Diğer text style'ları buraya eklenebilir
}
class AppStrings {
  static final AppStrings _instance = AppStrings._internal();

  factory AppStrings() {
    return _instance;
  }

  AppStrings._internal();

  // Metinler
  String get welcomeMessage => "Merhaba, Hoş Geldiniz!";
  String get loginButtonLabel => "Giriş Yap";
  String get registerButtonLabel => "Kayıt Ol";
  // Diğer metin değerleri buraya eklenebilir
}

class AppTextStyles {
  static final AppTextStyles _instance = AppTextStyles._internal();

  factory AppTextStyles() {
    return _instance;
  }

  AppTextStyles._internal();

  // Metin Stilleri
  TextStyle get welcomeMessageStyle => TextStyle(fontSize: 20, fontWeight: FontWeight.bold);
  TextStyle get loginButtonTextStyle => TextStyle(fontSize: 18, color: Colors.white);
  TextStyle get registerButtonTextStyle => TextStyle(fontSize: 18, color: Colors.blue);
  // Diğer text style'ları buraya eklenebilir
}
Text(
  AppStrings().welcomeMessage,
  style: AppTextStyles().welcomeMessageStyle,
)

FlatButton(
  onPressed: () {
    // Giriş yapma işlemi
  },
  child: Text(
    AppStrings().loginButtonLabel,
    style: AppTextStyles().loginButtonTextStyle,
  ),
)
 */
class AppStrings {
  static final AppStrings _instance = AppStrings._internal();

  factory AppStrings() {
    return _instance;
  }

  AppStrings._internal();

  // Metinler
  String get welcomeMessage => "Merhaba, Hoş Geldiniz!";
  String get loginButtonLabel => "Giriş Yap";
  String get registerButtonLabel => "Kayıt Ol";
  // Diğer metin değerleri buraya eklenebilir
}